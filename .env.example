# Copy to ".env" in the repo root and fill in values.
# ".env" is gitignored — do not commit it.
#
# OpenAI / compatible APIs (used by OASIS backend when installed with pip install "autofed[openai]")
OPENAI_API_KEY=
# Optional: override API base (Azure, proxy, local gateway)
# OPENAI_BASE_URL=
